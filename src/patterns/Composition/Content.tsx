import React from "react";

const Content = () => {
  return (
    <>
      <p>Check the code for this example</p>
      <p>
        Note that index.tsx is the entry point for the example and it doesn't do
        anything else that just rendering certain components in specific order.
      </p>
      <p>Each component have a concrete very specific task and it can be easly replaced without affecting the other</p>
    </>
  );
};

export default Content;
